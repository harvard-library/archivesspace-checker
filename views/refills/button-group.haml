-# Button Groups
-#   Takes locals: name, buttons
-#   Reference: http://refills.bourbon.io/components/#button-group

.button-group
  - buttons.each do |btn|
    %label
      - if btn.key? :checked
        %input{type: "radio", name: name, value: btn[:value], checked: btn[:checked]}
      - else
        %input{type: "radio", name: name, value: btn[:value]}
      %span.button-group-item= btn[:name]
